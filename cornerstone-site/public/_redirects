# The OS Sprint page was folded into the single landing page. The URL was
# indexed and linked, so it redirects rather than 404s.
/the-os-sprint  /  301
/the-os-sprint/ /  301

# Short aliases for the newsletter. Ads and anything spoken out loud want a URL
# you can say once; the canonical page keeps the full brand name.
/newsletter   /simple-systems-saturday  301
/newsletter/  /simple-systems-saturday  301
/saturday     /simple-systems-saturday  301
/saturday/    /simple-systems-saturday  301

# The Workshop was retired before it launched and the page is gone. The URL was
# linked from the nav and both footers, so it redirects to the offer that
# replaced it rather than 404s.
/workshop   /build  301
/workshop/  /build  301

# The resource library. The canonical path is /resources; these are the short
# names it gets called by out loud and in posts.
#
# ⚠ resources.cornerstone-ai.pro is a Cloudflare CUSTOM DOMAIN on this Pages
# project plus one Redirect Rule ("/" on that host -> /resources). It cannot be
# done here: _redirects matches paths, not hostnames, so a rule written here
# would fire on the apex domain too.
/library    /resources  301
/library/   /resources  301
/free       /resources  301
/free/      /resources  301
